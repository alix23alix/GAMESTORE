// Example JavaScript for handling the "Install" button
document.querySelectorAll('.app button').forEach(button => {
    button.addEventListener('click', () => {
        alert("App Installed!");
    });
});
